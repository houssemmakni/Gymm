{# templates/navbar.html.twig #}

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('app_reclamation_index') }}">
        Gym Reclamation
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_reclamation_table') }}">Reclamations</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_reclamation_new') }}">New Reclamation</a>
            </li>
        </ul>
    </div>
</nav>

<!-- Add the following CSS code to the head of your HTML document -->
<style>
    /* Additional Styles for Navbar */
    .navbar {
        background-color: #f8f9fa;
        transition: background-color 0.3s ease-in-out;
    }

    .navbar:hover {
        background-color: #e9ecef;
    }

    .navbar-brand {
        font-size: 1.8rem;
        font-weight: bold;
        color: #3498db;
        transition: color 0.3s ease-in-out;
    }

    .navbar-brand:hover {
        color: #207bca;
    }

    .navbar-nav .nav-link {
        color: #555;
        transition: color 0.3s ease-in-out;
    }

    .navbar-nav .nav-link:hover {
        color: #333;
    }

    .navbar-toggler {
        border: none;
        outline: none;
    }

    .navbar-toggler-icon {
        background-color: #3498db;
    }
</style>

<!-- Your existing JavaScript code for smooth scroll goes here -->
<script>
    document.addEventListener("DOMContentLoaded", function () {
        // Smooth scroll for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();

                document.querySelector(this.getAttribute('href')).scrollIntoView({
                    behavior: 'smooth'
                });
            });
        });
    });
</script>
